import React from "react";
import styled from "styled-components";

const TMP_ = () => {
  return (
    <Container>
      <InnerContainer>
        <div className="row">TMP_</div>
      </InnerContainer>
    </Container>
  );
};

export default TMP_;

const Container = styled.div``;

const InnerContainer = styled.div`
  margin: 0 auto;
  max-width: 1130px;
  padding: 0 30px;
`;
